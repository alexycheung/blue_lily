<%= simple_form_for :unit, url: checkin_reservations_path, method: :post, html: { class: "pure-form pure-form-stacked", id: "checkin-unit" } do |f| %>
	<div class="form-inputs">
		<%= f.input :unit_id, autofocus: true, label: "Unit ID" %>
	</div>
	<div class="form-actions">
		<%= f.button :submit, "Checkin", class: "pure-button pure-button-primary large round" %>
	</div>
<% end %>
