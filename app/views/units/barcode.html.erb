<div id="units-barcode">
	<div class="barcode">
		<%= unit_barcode(@unit) %>
		<div class="unit-id">UNIT #<%= @unit.id %></div>
		<div class="item-name"><%= truncate(@item.name, length: 40) %></div>
	</div>
</div>
